version: '3.8'

services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: flask_app
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/home/cpatwadityasharma/AdityaBackup/medical
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
